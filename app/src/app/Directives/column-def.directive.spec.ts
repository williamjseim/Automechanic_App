import { ColumnDefDirective } from './column-def.directive';

describe('ColumnDefDirective', () => {
  it('should create an instance', () => {
    const directive = new ColumnDefDirective();
    expect(directive).toBeTruthy();
  });
});
